{
	"experiment_description": "smoke_test_ensemble",
	"image_path": "/data/images/",
	"seed": 42,

	"ac_loss_weight": 1,
	"ver_loss_weight": 1,

	"ver_ensemble_size": 3,
	"ver_restart_every": 1,
	"ver_warmup_iters": 50,
	"ver_active_per_step": 1,

	"generator_type": "flow_field",
	"mu": 0.01,

	"image_size": 256,
	"batch_size": 16,
	"learning_rate": 0.0001,
	"max_epochs": 3,

	"show_every_n_epochs": 100,
	"show_every_n_iterations": 100
}
